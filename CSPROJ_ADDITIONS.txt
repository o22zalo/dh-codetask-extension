=== ADD to <ItemGroup> Compile section in DhCodetaskExtension.csproj ===

    <!-- v3.4 new files -->
    <Compile Include="Commands\ShowProjectHelperWindow.cs" />
    <Compile Include="ViewModels\ProjectHelperViewModel.cs" />
    <Compile Include="ToolWindows\ProjectHelperToolWindow.cs" />
    <Compile Include="ToolWindows\ProjectHelperControl.xaml.cs">
      <DependentUpon>ProjectHelperControl.xaml</DependentUpon>
    </Compile>

=== ADD to <ItemGroup> Page section ===

    <Page Include="ToolWindows\ProjectHelperControl.xaml">
      <SubType>Designer</SubType>
      <Generator>MSBuild:Compile</Generator>
    </Page>

=== NOTE: ProjectHelperToolWindow.cs is now merged into DevTaskToolWindows.cs ===
    No separate file needed — already included in DevTaskToolWindows.cs update.
    Remove the separate ProjectHelperToolWindow.cs entry if present.

=== REMOVED from Compile (v3.4 cleanup — SolutionFiles moved out of Tracker) ===
    TrackerViewModel.cs no longer references SolutionFileEntry or SolutionFileService
    TrackerControl.xaml.cs no longer has SolutionFile* handlers or SolutionExtIconConverter
    → SolutionExtIconConverter is now only in ProjectHelperControl.xaml.cs (as ExtBgConverter / ExtLabelConverter)
